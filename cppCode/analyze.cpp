#include "analyze.h"

std::vector<ClassOut> analyze(std::vector<ClassIn> inputVec) {
  std::cout << inputVec.size() << std::endl;
  std::vector<ClassOut> outputVec;
  outputVec.push_back(ClassOut());
  outputVec.push_back(ClassOut());
  outputVec.push_back(ClassOut());
  return outputVec;
}
